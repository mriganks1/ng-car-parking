import { FilterSlotsPipe } from './filter-slots.pipe';

describe('FilterSlotsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSlotsPipe();
    expect(pipe).toBeTruthy();
  });
});
